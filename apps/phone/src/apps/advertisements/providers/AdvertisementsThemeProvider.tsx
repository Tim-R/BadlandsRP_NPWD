import { createAppThemeProvider } from '@os/apps/utils/createAppThemeProvider';
import advertisementsTheme from '../advertisements.theme';

export const AdvertisementsThemeProvider = createAppThemeProvider(advertisementsTheme);
