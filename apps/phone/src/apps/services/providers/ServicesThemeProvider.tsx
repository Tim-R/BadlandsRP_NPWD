import { createAppThemeProvider } from '@os/apps/utils/createAppThemeProvider';
import servicesTheme from '../services.theme';

export const ServicesThemeProvider = createAppThemeProvider(servicesTheme);
