import { createAppThemeProvider } from '@os/apps/utils/createAppThemeProvider';
import bleeterTheme from '../bleeter.theme';

export const BleeterThemeProvider = createAppThemeProvider(bleeterTheme);
